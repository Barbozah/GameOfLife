package Business;

import Gui.MainFrame;

public class Testes {

	public static void main(String[] args) {
		
		new MainFrame();
		/*
		SimpleSimulation sim = new SimpleSimulation(20, 60);
		Player p = new Player(Player.CURSOR_LIVE, 0);
		
		sim.setVelocity(4500);
		
		sim.mutation(p.getCursorBehavior(), 1, 2);
		sim.mutation(p.getCursorBehavior(), 2, 2);
		sim.mutation(p.getCursorBehavior(), 3, 2);
		
		sim.mutation(p.getCursorBehavior(), 10, 4);
		sim.mutation(p.getCursorBehavior(), 10, 5);
		sim.mutation(p.getCursorBehavior(), 10, 6);
		
		sim.play();
		*/
	}

}
